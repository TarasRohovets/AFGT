@model IEnumerable<AFGT.Models.Evento>

@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <div class="row">
        <div class="col-md-offset-2 col-md-10">
            <input type="text" class="form-control conteudo" />
            <button class="btn btn-default">Ir</button>
            <select>
                <option class="search">Artista</option>
                <option class="search">Genero</option>
            </select>
        </div>
    </div>
    <h1>AFGT</h1>
    <p class="lead">AFGT guia-te pelo o que ouves! xD</p>
</div>


<div class="row">
    <p>Pesquisar por:</p>
    <select>
        <option class="option">Data</option>
        <option class="option">Local</option>
    </select>
    <input type="date" class="form-control tipo" placeholder="dd/mm/aaaa"/> 
    <input type="text" class="form-control tipo not-showing" placeholder="Localizacao" /> 
    <br>
    <br>  
    @{
        var SortedEvent = Model.Data.sort();
        foreach (var evento in SortedEvent)
        {
            <div class="col-md-4">
                <p> evento.NomeEvento </p> @*bla bla bla*@
            </div>
        }
    }   
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

    <script>
        var TipoPesquisa = $('.search').val();
        var ConteudoPesquisa = $('.conteudo').text;
        var TipoOpcao = $('.option').val();

    $('button').click(function () {
        while (!ConteudoPesquisa.length) {
            if (TipoOpcao == "Local") {
                //adicionar a classe do not-showing na data
                //remover a classe do not-showing
                //definir a  var dist
                // definir SortedEvent
            } else {
                SortedEvent;
            }
        }

        switch (TipoPesquisa) {
            case "Artista":
                if (TipoOpcao == "Local") {
                    var dist;
                } else {
                    SortedEvent = Model.Artista.All(ConteudoPesquisa).Data.sort();
                }
                break;

            case "Genero":
                if (TipoOpcao == "Local") {
                    var dist;
                } else {
                    SortedEvent = Model.Genero.All(ConteudoPesquisa).Data.sort();
                }
                break;
        }
    })
    </script>

}