@model IEnumerable<AFGT.Models.Artista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LinkFoto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GeneroMusical.NomeEstilo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LinkFoto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GeneroMusical.NomeEstilo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtistasID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtistasID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtistasID })
        </td>
    </tr>
}

</table>
